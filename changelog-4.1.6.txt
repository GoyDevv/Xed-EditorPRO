Xed-Editor PRO — Changelog
Version 4.1.6 (versionCode 113)

AI Agent — more tools.

>>> BUILD NOTE: does NOT build locally (no JDK 21 / SDK 37 / NDK in our dev env). Verified ONLY by
>>> GitHub Actions CI.

================================================================================
NEW in 4.1.6
================================================================================
- core/main/src/main/java/com/rk/ai/AiTools.kt (edited) — three new agent tools (schema + describe()
  + execute()), each permission-gated like the rest:
    • search_text(query, path?) — recursive, case-insensitive text search (skips .git/node_modules/
      build/.gradle/.cxx, caps at 200 matches, ignores files >1 MB). Returns file:line: snippet.
    • delete_file(path)         — delete a file or directory (deleteRecursively).
    • move_file(from, to)       — move/rename (creates the destination parent).
  names now: read_file, write_file, list_dir, run_command, search_text, delete_file, move_file.
- app/build.gradle.kts: 4.1.5 -> 4.1.6 (versionCode 112 -> 113).
- phases.md: tools item updated (apply-patch/diff still remaining).

================================================================================
AI feature timeline (full detail in phases.md)
================================================================================
4.1.0 engine+UI · 4.1.1 persistence · 4.1.2 streaming · 4.1.3 stop/cancel · 4.1.4 bg notification ·
4.1.5 settings screen · 4.1.6 more tools (this file).

REMAINING (phases.md): persistent agent terminal across turns + per-command notification text + true
in-flight OkHttp Call cancel; apply-patch/diff tool; markdown/code-block rendering of replies; MCP (opt).
